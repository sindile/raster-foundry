package com.rasterfoundry.backsplash.export

import com.rasterfoundry.common.datamodel

object ExportTranslation {
  //def translate[SourceDefinition: Exportable](
  //    ed: datamodel.ExportDefinition): ExportDefinition = {
  //  val outputDefinition =
  //    OutputDefinition(ed.output.crs,
  //                     ed.output.source,
  //                     ed.output.dropboxCredentials)

  //  val sourceDefinition: SourceDefinition = ed.input match {
  //    case SimpleInput(layers, mask) =>
  //      MosaicExportSource(zoom, area, layers)
  //    case ASTInput(ast, ingestLocs, projectScenes) =>
  //      AnalysisExportSource()
  //  }

  //  ExportDefinition(ed.id, sourceDefinition, outputDefinition)
  //}
}
